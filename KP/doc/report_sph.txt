Класс Player:
		- ник игрока
		- vector быков
		- vector коров
		- число игрока

	Класс Session:
		- имя сессии
		- макс. количество игроков
		- загаданное число
		- игроки (массив Player)

client:
	игрок может:
 		- создать сессию (просто вводит имя и количество игроков)
 			1. создать объект общей памяти, туда отобразить класс Session, в котором все параметры по нулям, имя присвоить именем сессии, количество игроков - количеством игроков, введенные клиентом.
 		- присоединиться к уже существующей сессии (по имени)
 		- начать поиск свободной сессии

 	Должен присутствовать 1 объект общей памяти, играющий роль Pipe, в который будем отображать Session. 
 	скорее всего, нужен semaphore / condvar / mutex, блокировать его когда поступил запрос на создание сессии.
 	при этом когда session отображается, семафор блокируется.
server:
	Должен присутствовать 1 объект общей памяти, в который отобразить 
	бесконечный цикл, в котором сервер:
		- если mutex заблокирован, то:
			 

Идеи
	Создать несколько объектов общей памяти для каждой сессии
	
#TODO
    1. dfs in sessionMap
    